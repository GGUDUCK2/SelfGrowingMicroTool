1. Add CSS Forge to `registry.json` and `dictionaries.ts`.
2. Create `types.ts` for CSS Forge state and dictionary types.
3. Create `CssWorkspace.svelte` and `CssHistory.svelte` under `src/lib/components/css-forge`.
4. Create the main route page `src/routes/[lang]/tools/css-forge/+page.svelte` adhering to SEO, JSON-LD, `FAQSection`, `AdPlaceholder`, and `RelatedTools`.
5. Run linting and build checks to ensure stability.
6. Check navigation integrity for the newly created tool.
7. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
8. Submit the final definitive tool.
