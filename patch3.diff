--- src/lib/components/hash-forge/HmacGenerator.svelte
+++ src/lib/components/hash-forge/HmacGenerator.svelte
@@ -95,6 +95,16 @@
       message = '';
       secret = '';
       hmacResult = null;
+    } else if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') {
+      e.preventDefault();
+      handleInput();
+    } else if (e.key === 'Escape') {
+      message = '';
+      secret = '';
+      hmacResult = null;
+    } else if ((e.ctrlKey || e.metaKey) && e.key === 's') {
+      e.preventDefault();
+      saveCurrentToHistory();
     }
   }
